package com.philips.resume.repository;

import com.philips.resume.domain.ResumeExperienceProjectAccomplish;

import org.springframework.data.jpa.repository.*;

import java.util.List;

/**
 * Spring Data JPA repository for the ResumeExperienceProjectAccomplish entity.
 */
public interface ResumeExperienceProjectAccomplishRepository extends JpaRepository<ResumeExperienceProjectAccomplish,Long> {

}
